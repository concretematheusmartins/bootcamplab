version: '3'

networks:
     homolog-network:
       driver: bridge

services:
    web1:
        image: httpd:latest
        restart: always
        networks:
            - homolog-network
        ports:
            - 8083:80
        volumes:
            - ./:/usr/local/apache2/htdocs/